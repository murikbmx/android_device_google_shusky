<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<resources>
    <dimen name="status_bar_content_padding_start">0dp</dimen>

    <dimen name="status_bar_content_padding_end">0dp</dimen>
    
    <dimen name="rounded_corner_content_padding">0dp</dimen>
    
    <!-- the padding on the start of the statusbar -->
    <dimen name="status_bar_padding_start">3dp</dimen>
    
    <!-- the padding on the end of the statusbar -->
    <dimen name="status_bar_padding_end">3dp</dimen>

    <!-- Padding for the lock icon on the keyguard. In pixels - should not scale with display size. -->
    <dimen name="lock_icon_padding">50px</dimen>

    <!-- Margin on the left side of the carrier text on Keyguard -->
    <dimen name="keyguard_carrier_text_margin">5dp</dimen>

    <!-- The maximum offset for the under-display fingerprint sensor (UDFPS) icon in either
         direction that elements are moved to prevent burn-in on AOD-->
    <dimen name="udfps_burn_in_offset_x">6px</dimen>
    <dimen name="udfps_burn_in_offset_y">17px</dimen>

    <!--  Total minimum padding to enforce to ensure that the dot can always show  -->
    <dimen name="ongoing_appops_dot_min_padding">16dp</dimen>

    <!-- Location on the screen of the center of the physical power button. This is a reasonable
    default that should be overridden by device-specific overlays. -->
    <dimen name="physical_power_button_center_screen_location_y">698px</dimen>

    <!-- Microns/ums (1000 um = 1mm) per pixel for the given device. If unspecified, UI that
         relies on this value will not be sized correctly. -->
    <item name="pixel_pitch" format="float" type="dimen">59.5</item>

    <!-- Weather clock smartspace scaling to apply for the weather clock -->
    <dimen name="weather_clock_smartspace_translateY">10dp</dimen>
</resources>
